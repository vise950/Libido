name: libido
description: A new Flutter project.
publish_to: 'none'

version: 1.0.3+4

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  easy_localization: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  mockito: ^5.1.0

flutter:
  uses-material-design: true

  assets:
    - assets/json/
    - assets/images/
    - assets/translations/